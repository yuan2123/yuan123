# 星火财商后台双模块修改说明

## 本次完成内容

管理后台已升级为两个独立模块：

1. **课程报名**：继续管理 `/register` 提交的报名数据。
2. **咨询留言**：管理官网首页“提交咨询”表单的数据。

咨询留言模块现已支持：

- 姓名、联系方式、关注方向、留言搜索
- 待联系、已联系、已完成、无效咨询状态筛选
- 修改跟进状态
- 删除前二次确认
- 刷新列表
- 导出带 UTF-8 BOM 的 CSV，Excel 打开中文不乱码
- 未登录自动跳转 `/admin/login`
- 手机端横向滚动查看表格

## 修改和新增文件

- 修改：`app/admin/page.tsx`
- 修改：`app/api/contact/route.ts`
- 新增：`app/api/contact/[id]/route.ts`
- 新增：`lib/contact-inquiry.ts`
- 修改：`app/globals.css`
- 修改：`.gitignore`

## 数据库

`ContactInquiry` 现有字段已满足需求，因此：

- 未修改 `prisma/schema.prisma`
- 不需要创建或执行新的数据库迁移
- 不会清空或覆盖 Neon 中的现有数据

## API

- `POST /api/contact`：公众提交咨询
- `GET /api/contact`：管理员查询咨询列表
- `GET /api/contact/:id`：管理员查询单条咨询
- `PATCH /api/contact/:id`：管理员修改咨询状态
- `DELETE /api/contact/:id`：管理员删除咨询

## 上传 GitHub

可以上传整个项目覆盖同名文件，也可以只上传以下 5 个业务文件：

- `app/admin/page.tsx`
- `app/api/contact/route.ts`
- `app/api/contact/[id]/route.ts`
- `lib/contact-inquiry.ts`
- `app/globals.css`

保持原目录层级，提交后 Vercel 会自动部署。

## 部署后测试

1. 打开官网并提交首页咨询表单。
2. 登录 `https://xhcs.vercel.app/admin/login`。
3. 点击“咨询留言”，确认刚提交的数据出现。
4. 测试状态修改、搜索、删除和 CSV 导出。
5. 打开 `https://xhcs.vercel.app/register` 提交报名，确认“课程报名”模块仍正常。
