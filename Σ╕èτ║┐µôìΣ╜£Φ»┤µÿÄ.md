# 星火财商：不用命令行的上线操作说明

目标：获得一个电脑、手机、微信都能打开的 HTTPS 地址，并让报名数据保存到云数据库。

## 第一步：上传到 GitHub

1. 登录 GitHub，创建一个新的 Private 仓库，名称建议 `spark-wealth-academy`。
2. 不要勾选自动创建 README、.gitignore 或 License。
3. 解压本项目压缩包。进入解压后的项目文件夹，确认最外层能看到 `package.json`、`app`、`components`、`prisma`。
4. 在 GitHub 仓库页面点击 `Add file` → `Upload files`。
5. 把项目文件夹内的全部文件和文件夹拖入网页，不要把外层总文件夹或 ZIP 文件直接上传。
6. 页面底部点击 `Commit changes`。

## 第二步：在 Vercel 创建网站

1. 登录 Vercel，选择使用 GitHub 登录。
2. 点击 `Add New` → `Project`。
3. 找到刚才的 `spark-wealth-academy` 仓库，点击 `Import`。
4. Framework Preset 保持 `Next.js`，Root Directory 保持 `./`。
5. 先不要点击 Deploy，先创建数据库。

## 第三步：创建 Prisma Postgres 数据库

1. 在 Vercel 项目配置页或项目创建后进入 `Storage`。
2. 点击 `Create Database`，选择 `Prisma Postgres`。
3. 选择离主要访问用户较近的 Region，创建数据库。
4. 将数据库 Connect 到本项目。完成后，Vercel 会自动加入 `DATABASE_URL`。

## 第四步：配置后台环境变量

在项目的 `Settings` → `Environment Variables` 中添加：

- `ADMIN_USERNAME`：你自己设定的后台用户名。
- `ADMIN_PASSWORD`：至少 12 位，包含大小写字母、数字和符号。
- `ADMIN_SESSION_SECRET`：32 位以上随机字符串。
- `REGISTRATION_DUPLICATE_WINDOW_MS`：填写 `60000`。

这四项都勾选 Production、Preview、Development。不要把密码写进代码或发到公开群。

## 第五步：部署

1. 点击 `Deploy`。
2. 构建过程中会自动运行数据库迁移并创建报名表。
3. 成功后，Vercel 会生成一个 `https://项目名.vercel.app` 地址。

## 第六步：验收

依次打开：

- 首页：`https://你的地址/`
- 课程报名：`https://你的地址/register`
- 数据库检查：`https://你的地址/api/health`，应显示 `database: connected`。
- 后台登录：`https://你的地址/admin/login`

用手机流量和微信分别打开报名页，提交一条测试数据，再登录后台检查是否出现。

## 第七步：绑定正式域名（测试通过后）

进入 Vercel 项目 `Settings` → `Domains` → `Add Domain`，输入你的正式域名，然后按照页面提示去域名服务商配置 DNS。绑定完成后，在环境变量中添加：

`NEXT_PUBLIC_SITE_URL=https://你的正式域名`

保存后重新 Redeploy。

## 常见错误

- 构建提示 `DATABASE_URL` 缺失：数据库尚未 Connect 到项目。
- 构建提示 Prisma migration 失败：检查 Storage 中数据库是否正常，并重新部署。
- 后台始终登录失败：检查三个 ADMIN 环境变量是否都已设置，修改环境变量后必须 Redeploy。
- 报名成功但后台没有数据：确认访问的是同一个 Vercel 项目和同一个数据库。
